// PDF Conversion Service
// Real conversion logic will be added in the next step.

async function pdfToWord(inputPath, outputPath) {
  console.log("Converting:", inputPath);

  return outputPath;
}

module.exports = {
  pdfToWord,
};